# 📊 ПОЛНЫЙ МАППИНГ ПОЛЕЙ: IDENT → BITRIX24

## 🗄️ Поля из БД Ident (SQL запрос)

### 📋 Данные записи на прием
| Поле в Ident | Тип | Описание |
|--------------|-----|----------|
| `ReceptionID` | int | ID записи |
| `StartTime` | datetime | Плановое время начала приема |
| `EndTime` | datetime | Плановое время окончания приема |

---

### 👤 Данные пациента
| Поле в Ident | Тип | Описание |
|--------------|-----|----------|
| `PatientFullName` | string | Полное ФИО (Фамилия Имя Отчество) |
| `PatientSurname` | string | Фамилия пациента |
| `PatientName` | string | Имя пациента |
| `PatientPatronymic` | string | Отчество пациента |
| `CardNumber` | string | Номер карты пациента |
| `PatientPhone` | string | Мобильный телефон |

---

### 👨‍⚕️ Данные врача
| Поле в Ident | Тип | Описание |
|--------------|-----|----------|
| `DoctorFullName` | string | Полное ФИО врача |
| `DoctorSurname` | string | Фамилия врача |
| `DoctorName` | string | Имя врача |
| `DoctorPatronymic` | string | Отчество врача |
| `Speciality` | string | Специальность врача (напр: "Стоматолог-терапевт") |

---

### 🏥 Филиал и кабинет
| Поле в Ident | Тип | Описание |
|--------------|-----|----------|
| `Filial` | string | Название филиала (напр: "Филиал №1") |
| `Armchair` | string | Название кабинета (напр: "Кабинет 5") |

---

### 💊 Услуги и оплата
| Поле в Ident | Тип | Описание |
|--------------|-----|----------|
| `Services` | string | Список услуг через запятую (макс 3000 символов) |
| `TotalAmount` | decimal | Общая сумма услуг с учетом скидки |

---

### 📊 Статус записи
| Поле в Ident | Тип | Описание |
|--------------|-----|----------|
| `Status` | string | Текущий статус (вычисляемое поле) |
| `PatientAppeared` | datetime | Время когда пациент пришел |
| `ReceptionStarted` | datetime | Время начала приема |
| `ReceptionEnded` | datetime | Время окончания приема |
| `ReceptionCanceled` | datetime | Время отмены приема |
| `CheckIssued` | datetime | Время выдачи счета |

**Логика вычисления Status:**
- `ReceptionCanceled IS NOT NULL` → "Отменен"
- `CheckIssued IS NOT NULL` → "Завершен (счет выдан)"
- `ReceptionEnded IS NOT NULL` → "Завершен"
- `ReceptionStarted IS NOT NULL` → "В процессе"
- `PatientAppeared IS NOT NULL` → "Пациент пришел"
- `ELSE` → "Запланирован"

---

### 📅 Даты и время
| Поле в Ident | Тип | Описание |
|--------------|-----|----------|
| `OrderDate` | datetime | Дата и время создания заказа |
| `BillFormedDate` | datetime | Дата формирования счета |
| `CreatedAt` | datetime | Дата создания записи |
| `ChangedAt` | datetime | Дата последнего изменения |

---

### 💬 Комментарий
| Поле в Ident | Тип | Описание |
|--------------|-----|----------|
| `Comment` | string | Комментарий к записи |

---

## 🔄 МАППИНГ В BITRIX24

### 1️⃣ Контакт (Contact)
| Поле в Ident | Поле в Bitrix24 | Преобразование |
|--------------|-----------------|----------------|
| `PatientName` | `NAME` | Как есть |
| `PatientSurname` | `LAST_NAME` | Как есть |
| `PatientPatronymic` | `SECOND_NAME` | Как есть |
| `PatientPhone` | `PHONE[0].VALUE` | Нормализация: +7XXXXXXXXXX |
| - | `PHONE[0].VALUE_TYPE` | Константа: "MOBILE" |
| - | `TYPE_ID` | Константа: "CLIENT" |

---

### 2️⃣ Сделка (Deal) - Основные поля
| Поле в Ident | Поле в Bitrix24 | Преобразование |
|--------------|-----------------|----------------|
| `PatientFullName` | `TITLE` | Как есть (ФИО пациента) |
| `Status` | `STAGE_ID` | Через StageMapper (см. ниже) |
| `TotalAmount` | `OPPORTUNITY` | Decimal → float |
| - | `CURRENCY_ID` | Константа: "RUB" |
| - | `CONTACT_ID` | ID созданного контакта |

---

### 3️⃣ Сделка - Кастомные поля Bitrix24
| Поле в Ident | Поле в Bitrix24 | ID поля | Преобразование |
|--------------|-----------------|---------|----------------|
| `StartTime` | Дата начала приема | `UF_CRM_1769008900` | datetime → ISO 8601 |
| `EndTime` | Дата окончания приема | `UF_CRM_1769008947` | datetime → ISO 8601 |
| `DoctorFullName` | Врач | `UF_CRM_1769008996` | Как есть |
| `Services` | Услуги | `UF_CRM_1769009098` | Обрезка до 3000 символов |
| `F{FilialID}_{ReceptionID}` | ID из Ident | `UF_CRM_1769072841035` | Уникальный ID (напр: F1_12345) |

---

### 4️⃣ Сделка - Комментарий (COMMENTS)
Комментарий формируется из нескольких полей:

```
📅 Прием: {StartTime}
👨‍⚕️ Врач: {DoctorFullName} ({Speciality})
🏥 Филиал: {Filial}
🪑 Кабинет: {Armchair}
💊 Услуги: {Services} (обрезка до 200 символов)
💰 Сумма: {TotalAmount} ₽

📝 Комментарий: {Comment}

📊 Статус: {Status}
🔗 ID: F{FilialID}_{ReceptionID}
```

---

## 🎯 МАППИНГ СТАДИЙ (StageMapper)

| Статус в Ident | Стадия в Bitrix24 | ID стадии |
|----------------|-------------------|-----------|
| Запланирован | Запись на консультацию | `NEW` |
| Пациент пришел | Запись на консультацию | `NEW` |
| В процессе | Консультация проведена | `PREPARATION` |
| Завершен | Лечение | `UC_NO40X0` |
| Завершен (счет выдан) | ✅ Сделка успешна | `WON` |
| Отменен | ❌ Сделка провалена | `LOSE` |

---

## 🚫 ПОЛЯ КОТОРЫЕ НЕ ОТПРАВЛЯЮТСЯ В BITRIX24

Следующие поля извлекаются из БД, но **НЕ отправляются** напрямую в Bitrix24:

| Поле | Почему не отправляется | Как используется |
|------|------------------------|------------------|
| `ReceptionID` | - | Используется для генерации unique_id |
| `DoctorSurname`, `DoctorName`, `DoctorPatronymic` | - | Собираются в DoctorFullName |
| `PatientAppeared` | - | Используется для определения статуса |
| `ReceptionStarted` | - | Используется для определения статуса |
| `ReceptionEnded` | - | Используется для определения статуса |
| `ReceptionCanceled` | - | Используется для определения статуса |
| `CheckIssued` | - | Используется для определения статуса |
| `BillFormedDate` | - | Не используется в текущей логике |
| `CreatedAt` | - | Используется для инкрементальной синхронизации |
| `ChangedAt` | - | Используется для инкрементальной синхронизации |
| `CardNumber` | - | Показывается только в комментарии (исключено из raw_data) |

---

## ✅ ИТОГОВЫЙ СПИСОК ИСПОЛЬЗУЕМЫХ ПОЛЕЙ

### В Контакте Bitrix24:
1. `NAME` ← PatientName
2. `LAST_NAME` ← PatientSurname
3. `SECOND_NAME` ← PatientPatronymic
4. `PHONE` ← PatientPhone (нормализованный)
5. `TYPE_ID` = "CLIENT"

### В Сделке Bitrix24:
1. `TITLE` ← PatientFullName
2. `STAGE_ID` ← Status (через маппинг)
3. `OPPORTUNITY` ← TotalAmount
4. `CURRENCY_ID` = "RUB"
5. `CONTACT_ID` ← ID созданного контакта
6. `UF_CRM_1769008900` ← StartTime
7. `UF_CRM_1769008947` ← EndTime
8. `UF_CRM_1769008996` ← DoctorFullName
9. `UF_CRM_1769009098` ← Services
10. `UF_CRM_1769072841035` ← Unique ID (F1_12345)
11. `COMMENTS` ← Составной комментарий

---

## 📌 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

### Нормализация телефона:
```
+7 (999) 123-45-67 → +79991234567
8 999 123 45 67    → +79991234567
9991234567         → +79991234567
```

### Формат дат (ISO 8601):
```
2024-01-15 14:30:00 → 2024-01-15T14:30:00+03:00
```

### Уникальный ID:
```
Филиал 1, Запись 12345 → F1_12345
Филиал 3, Запись 67890 → F3_67890
```

---

## 🔮 ПОТЕНЦИАЛЬНЫЕ ДОПОЛНИТЕЛЬНЫЕ ПОЛЯ

Поля из БД которые **МОЖНО** добавить в Bitrix24:

| Поле | Где использовать | Зачем |
|------|------------------|-------|
| `CardNumber` | Новое кастомное поле | Номер карты пациента для быстрого поиска |
| `BillFormedDate` | Новое кастомное поле | Дата формирования счета |
| `OrderDate` | Новое кастомное поле | Дата создания заказа |
| `Armchair` | Новое кастомное поле | Кабинет для планирования |
| `Filial` | Новое кастомное поле | Филиал для аналитики |
