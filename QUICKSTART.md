# Быстрый старт - Развертывание интеграции Ident-Битрикс24

## За 30 минут от нуля до работающей интеграции

---

## 1. Подключение к серверу (2 мин)

```
Откройте RDP:
Win + R → mstsc
Введите IP сервера и учетные данные
```

---

## 2. Установка Python (5 мин)

1. Скачайте: https://www.python.org/downloads/
2. Запустите установщик
3. ✅ Отметьте: "Add Python to PATH"
4. Установите в: `C:\Python310`

Проверка:
```powershell
python --version
```

---

## 3. Установка ODBC Driver (3 мин)

1. Скачайте: https://learn.microsoft.com/sql/connect/odbc/download-odbc-driver-for-sql-server
2. Запустите `msodbcsql.msi`
3. Установите с настройками по умолчанию

---

## 4. Развертывание проекта (5 мин)

### Вариант A: Через Git
```powershell
cd C:\
mkdir Projects
cd Projects
git clone https://github.com/sadasdadsadasdw-spec/IDENT.git
cd IDENT
```

### Вариант B: Копирование файлов
```powershell
# 1. Перетащите архив проекта через RDP
# 2. Распакуйте в C:\Projects\IDENT
```

---

## 5. Установка зависимостей (3 мин)

```powershell
cd C:\Projects\IDENT
pip install -r requirements.txt
```

Ждите завершения...

---

## 6. Настройка конфигурации (5 мин)

```powershell
copy config.example.ini config.ini
notepad config.ini
```

**Заполните ОБЯЗАТЕЛЬНЫЕ поля:**

```ini
[Database]
server = localhost
database = IdentDB
username = ident_user
password = YOUR_DB_PASSWORD

[Bitrix24]
webhook_url = https://your-company.bitrix24.ru/rest/1/YOUR_TOKEN
token = YOUR_TOKEN

[Sync]
filial_id = 1  # ВАЖНО! Укажите номер филиала (1-5)
```

**Как получить webhook:**
1. Битрикс24 → Настройки → Интеграции → Входящие вебхуки
2. Создать webhook с правами CRM
3. Скопировать URL

---

## 7. Тест подключения (2 мин)

```powershell
python test_integration.py
```

Должно быть:
```
✓ Конфигурация загружена
✓ Логгер инициализирован
✓ Подключение установлено
✓ Статистика получена
✓ Получено записей: 5
```

---

## 8. Установка службы (5 мин)

```powershell
# Откройте PowerShell от администратора
python install_service.py install
python install_service.py --startup=auto install
python install_service.py start
```

Проверка:
```powershell
sc query IdentBitrix24Integration
```

Должно быть: `STATE: RUNNING`

---

## 9. Проверка работы (3 мин)

1. Откройте браузер: `http://localhost:8080`
2. Проверьте статус: должно быть "Running"
3. Дождитесь первой синхронизации (2-5 мин)
4. Проверьте Битрикс24 → CRM → Сделки

---

## 10. Готово! 🎉

Интеграция работает автоматически.

**Что происходит:**
- Каждые 2 минуты синхронизируются записи из Ident
- Создаются контакты и сделки в Битрикс24
- Логи пишутся в `C:\Projects\IDENT\logs\`

**Мониторинг:**
- Веб-интерфейс: http://localhost:8080
- Логи: `C:\Projects\IDENT\logs\integration_log_YYYY-MM-DD.txt`

---

## Что-то пошло не так?

### Ошибка подключения к БД:
```powershell
# Проверьте учетные данные
notepad config.ini
```

### Ошибка Битрикс24 (401/403):
- Проверьте webhook токен в Битрикс24
- Убедитесь что webhook активен
- Проверьте права доступа (нужны CRM права)

### Служба не запускается:
```powershell
# Запустите вручную для отладки
python main.py
# Смотрите ошибки в консоли
```

### Подробная документация:
Смотрите: `docs/DEPLOYMENT_GUIDE.md`

---

## Повторное развертывание на других филиалах

Для каждого следующего филиала:

1. Повторите шаги 1-6
2. **ВАЖНО:** Измените в `config.ini`:
   ```ini
   [Sync]
   filial_id = 2  # Для филиала 2
   filial_id = 3  # Для филиала 3
   # И т.д.
   ```
3. Повторите шаги 7-9

**Один webhook для всех филиалов!**
Используйте одинаковый webhook_url для всех 5 филиалов.

---

## Чеклист

Готовность к работе:

- [ ] Python установлен
- [ ] ODBC Driver установлен
- [ ] Проект развернут
- [ ] Конфигурация заполнена (БД, Битрикс24, filial_id)
- [ ] Тест подключения прошел успешно
- [ ] Служба установлена и запущена
- [ ] Веб-интерфейс доступен
- [ ] Первая синхронизация выполнена
- [ ] Данные появились в Битрикс24

---

*Время развертывания: ~30 минут*
*Требуемый уровень: базовые навыки работы с Windows Server*
